{
  "name": "@maze-runner/backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 0.0.0.0 --port 8787 --local",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "migrate": "wrangler d1 migrations apply maze-runner-db --local"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "vitest": "^2.1.8",
    "wrangler": "^4.4.0"
  }
}
